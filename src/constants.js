/**
 * @typedef {number} Constant
 */

const VERSION = '0.2.2';
const SUCCESS = 1;
const FAILURE = 2;
const RUNNING = 3;
const ERROR = 4;
const COMPOSITE = 'composite';
const DECORATOR = 'decorator';
const ACTION = 'action';
const CONDITION = 'condition';

module.exports = {
    VERSION,
    SUCCESS,
    FAILURE,
    RUNNING,
    ERROR,
    COMPOSITE,
    DECORATOR,
    ACTION,
    CONDITION
};
